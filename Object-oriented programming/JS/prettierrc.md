Особисті налаштування JavaScript для Prettier - Code formatter

```json
{
  "tabWidth": 4, // Кількість пробілів на один відступ (замість використання табуляції)
  "useTabs": false, // Використовувати пробіли замість табуляції
  "printWidth": 100, // Максимальна ширина рядка коду, після якої відбувається перенесення
  "breakWords": false, // Розривати слова, щоб забезпечити перенесення рядка, якщо потрібно
  "singleQuote": true, // Використовувати одинарні лапки замість подвійних для рядкових літералів
  "trailingComma": "all", // Додавати кому в кінці останнього елементу масиву або об'єкту
  "semi": true, // Додавати крапку з комою в кінці кожного оператора
  "bracketSpacing": true, // Додавати пробіл навколо внутрішніх дужок об'єктів
  "arrowParens": "always" // Додавати дужки навколо параметрів стрілочних функцій завжди
}
```
