let a = 10,
    b = 20,
    c = 30;
function sum() {
    let d = b - a;
    let e = c - b;
    let f = a + 0;
    return {
        text: 'Your result',
        result: [d, e, f],
    };
}

console.log(sum());

//спершу оголошуємо данні, далі взиваємо функцію, в функції присвоюємо нові дані, та операції з даними, після чого повертаємо результат.
// та виводимо його у консоль

// також можна вивести у вигляді таблиці
console.table(sum());
